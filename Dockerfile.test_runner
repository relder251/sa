FROM python:3.12-slim

RUN pip install --no-cache-dir flask requests

WORKDIR /app

# Script is bind-mounted at runtime from ./scripts/test_runner_server.py

CMD ["python", "/data/scripts/test_runner_server.py"]
