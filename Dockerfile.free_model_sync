FROM python:3.12-slim

RUN pip install --no-cache-dir requests

WORKDIR /app

# Script is bind-mounted at runtime from ./free_model_sync.py
# so no COPY needed — the image just provides the runtime.

CMD ["bash", "-c", "python free_model_sync.py && echo 'Initial sync done — ofelia handles scheduling.' && sleep infinity"]
